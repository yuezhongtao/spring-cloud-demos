eureka:
  instance:
    appname: actuator
    instance-id: actuator
  client:
    service-url: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
spring:
  application:
    name: actuator
server:
  port: 8083