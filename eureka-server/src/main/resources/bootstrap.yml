spring:
  application:
    name: eureka-server
  profiles:
    active: default
eureka:
  instance:
    prefer-ip-address: true
#  client:
#    service-url:
#      default-zone: http://user:password@localhost:8761/eureka/